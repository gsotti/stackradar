apiVersion: v1
kind: Service
metadata:
  name: {{ include "stackradar.fullname" . }}
  labels:
    {{- include "stackradar.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "stackradar.selectorLabels" . | nindent 4 }}
