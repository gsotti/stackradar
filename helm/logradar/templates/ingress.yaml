apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "logradar.fullname" . }}
  labels:
    {{- include "logradar.labels" . | nindent 4 }}
  annotations:
    cert-manager.io/cluster-issuer: {{ .Values.certIssuer | quote }}
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - {{ .Values.domain }}
      secretName: {{ include "logradar.fullname" . }}-tls
  rules:
    - host: {{ .Values.domain }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "logradar.fullname" . }}
                port:
                  number: {{ .Values.service.port }}
