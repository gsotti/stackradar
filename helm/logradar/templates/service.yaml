apiVersion: v1
kind: Service
metadata:
  name: {{ include "logradar.fullname" . }}
  labels:
    {{- include "logradar.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "logradar.selectorLabels" . | nindent 4 }}
