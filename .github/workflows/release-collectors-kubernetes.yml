name: Release Kubernetes Collectors

on:
  push:
    tags:
      - 'v*-collectors-kubernetes'

jobs:
  build-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    strategy:
      matrix:
        include:
          - name: stackradar-logs-collector
            context: collector-k8s/log-collector
          - name: stackradar-stats-collector
            context: collector-k8s/stats-collector

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Extract version
        id: version
        run: echo "VERSION=${GITHUB_REF_NAME%-collectors-kubernetes}" >> $GITHUB_OUTPUT

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push ${{ matrix.name }}
        uses: docker/build-push-action@v6
        with:
          context: ${{ matrix.context }}
          platforms: linux/amd64
          push: true
          build-args: |
            BUILD_VERSION=${{ steps.version.outputs.VERSION }}
            BUILD_COMMIT=${{ github.sha }}
          tags: |
            ghcr.io/gsotti/${{ matrix.name }}:${{ steps.version.outputs.VERSION }}
            ghcr.io/gsotti/${{ matrix.name }}:latest
